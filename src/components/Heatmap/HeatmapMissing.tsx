import { Link } from 'react-router-dom';

function HeatmapMissing() {
  return(
    <div>
      Visit <Link to="/">Data Loading</Link> to upload data
    </div>
  );
}

export default HeatmapMissing;
